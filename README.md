# BenettiMedia Chrome Extensions — landing pages

Static, problem-first landing pages for our Chrome Web Store extensions.
Generated by the make-exts pipeline (`npm run landing` + `gen-landing-site.mjs`).
Each page leads with a specific user pain and links to the live Web Store listing.
